<%- include('./partials/head') %>

<body>
    <div class="min-h-screen flex items-center justify-center bg-gray-100">
        <div class="mx-auto max-w-[80rem] bg-white p-8 shadow-md rounded-3xl space-y-4">
            <h1 class="text-2xl text-center">Hello, <span class="font-bold"><%= user.email %></span> </h1>
            <form action="/auth/logout" method="POST">
                <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-full w-full cursor-pointer">Logout</button>
            </form>
    </div>
</body>